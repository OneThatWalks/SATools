To use SATools and modify its code (giving proper credit) you simply download the source package and create a new project under your Java IDE with the location of the project being the unzipped/uncompressed directory.